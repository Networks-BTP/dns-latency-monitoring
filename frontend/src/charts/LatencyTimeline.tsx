import {LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer} from "recharts"; import type {NormalizedQuery} from "../types/dns";
export function LatencyTimeline({queries}:{queries:NormalizedQuery[]}){const data=queries.map((q,i)=>({i:i+1,latency:Number(q.latencyMs.toFixed(3)),domain:q.domain,resolver:q.resolver}));return <ResponsiveContainer width="100%" height={320}><LineChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="i"/><YAxis unit=" ms"/><Tooltip formatter={(v:number)=>[`${v} ms`,"Latency"]}/><Line type="monotone" dataKey="latency" stroke="currentColor" dot={false} strokeWidth={2}/></LineChart></ResponsiveContainer>}