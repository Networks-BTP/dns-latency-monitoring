import type {Dataset} from "../types/dns"; import {PageHeader} from "../components/PageHeader";
export function Datasets({datasets}:{datasets:Dataset[]}){return <div><PageHeader title="Datasets" description="Imported JSON runs remain individually identifiable while contributing to combined analysis."/><div className="panel table-wrap"><table><thead><tr><th>File</th><th>PID</th><th>Interface</th><th>Started</th><th>Queries</th><th>Resolver(s)</th></tr></thead><tbody>{datasets.map(d=><tr key={d.id}><td><strong>{d.fileName}</strong></td><td>{d.runPid}</td><td>{d.interfaceName}</td><td>{d.startedAt}</td><td>{d.queryCount}</td><td>{[...new Set(d.queries.map(q=>q.resolver))].join(", ")}</td></tr>)}</tbody></table></div></div>}